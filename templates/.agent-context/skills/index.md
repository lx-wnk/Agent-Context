# Skills Index

<!-- Skills are loaded on-demand when task keywords match their triggers. -->
<!-- Skill format: skills/<name>/SKILL.md with `name` + `description` frontmatter (Agent Skills standard, portable across agents). Legacy flat skills/<name>.md still works. See https://github.com/lx-wnk/Agent-Context/blob/main/docs/skill-standard.md. -->

| Skill           | Triggers                                                                 | Description                                                                                            |
| --------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------ |
| `discovery-map` | "discover/map the project", "/discover", onboarding, "where does X live" | On-demand concept map; fan-out discovery into a tiny `map.json` + `memory/<node>.md`. Never always-on. |

<!-- Add one row per skill, mirroring its SKILL.md name + description. -->
