{
  "name": "agent-context",
  "version": "1.0.0",
  "private": true,
  "description": "Layered, agent-agnostic context architecture for AI coding agents.",
  "scripts": {
    "prettier:fix": "prettier --write .",
    "prettier": "prettier --check .",
    "template-check": "bash scripts/check-template-coverage.sh"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
